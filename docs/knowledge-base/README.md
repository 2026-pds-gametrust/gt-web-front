# Knowledge Base (app)

A KB canônica do produto está na raiz: [`../../../../docs/knowledge-base/`](../../../../docs/knowledge-base/).

Este diretório pode manter `agent-context` local; decisões de tokens/paridade seguem a raiz (`KB-DEC-001` nativo).

Precedência: `AGENTS.md` deste app → raiz `AGENTS.md` → `context/` → KB raiz.
