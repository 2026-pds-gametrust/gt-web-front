# Recurso: trust-scores

Domínio: `trust`

| Método | Path | Contrato |
|--------|------|----------|
| `GET` | `/trust-scores/{sellerId}` | [Get trust score for seller (default 0)](./get-trust-scores-by-sellerId/) |
| `POST` | `/trust-scores/{sellerId}/recompute` | [Recompute trust score from ledger](./post-trust-scores-by-sellerId-recompute/) |
